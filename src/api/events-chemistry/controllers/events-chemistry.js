'use strict';

/**
 * events-chemistry controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::events-chemistry.events-chemistry');
